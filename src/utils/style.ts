import { StyleSheet } from 'react-native';

export const cStyle = StyleSheet.create({
  rowAlign: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  row: {
    flexDirection: 'row',
  },
  rowJustify: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
