import { StyleSheet } from 'react-native';

export const loginStyle = StyleSheet.create({
  absView: {
    flex: 1,
    height: 100,
    position: 'absolute',
    alignSelf: 'center',
    top: -10,
    zIndex: -10,
    opacity: 0.5,
  },
});
